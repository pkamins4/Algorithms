#ifndef INSERTION_SORT
#define INSERTION_SORT

#include<stdlib.h>

int* selectionSort(int* table, size_t size)
{
    int i=1;
    int q;   
    while(i<size)
    {
        q = table[i+1];
        for(int j=i; j>=0; j--)
        {
            if(q<table[j])
            {
                
            }

        }
    }
}
 




#endif